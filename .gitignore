node_modules/
dist/

.env
.env.local

# Uploaded originals, rendered pages, exported PDFs and the SQLite file are all
# runtime artefacts, not source.
server/storage/
server/data/
*.sqlite
*.sqlite-journal

.DS_Store
*.log
